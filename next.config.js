/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: [
            "staraszkola-biebrza.eu"
        ]
    }
}

module.exports = nextConfig
